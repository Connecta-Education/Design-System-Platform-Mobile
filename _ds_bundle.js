/* @ds-bundle: {"format":3,"namespace":"ConnectaDesignSystem_97ebf8","components":[],"sourceHashes":{},"inlinedExternals":[],"unexposedExports":[]} */

/*
 * Connecta Design System runtime bundle.
 *
 * The previous bundle exposed a Tamagui code export (XStack/YStack/NudgeBanner/
 * CoachCard/…). Tamagui has been removed from the design system — the component
 * layer is now PrimeReact, themed through the Connecta preset + `primereact-connecta.css`
 * (see README.md → "Component implementation"). PrimeReact is consumed directly
 * from the app, not bundled here, so this runtime exposes no live components.
 * The static design-system content (tokens, fonts, preview cards) lives in
 * `_ds_manifest.json`.
 */
(() => {

const __ds_ns = (window.ConnectaDesignSystem_97ebf8 = window.ConnectaDesignSystem_97ebf8 || {});
const __ds_scope = {};
(__ds_ns.__errors = __ds_ns.__errors || []);

})();
